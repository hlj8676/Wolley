/*
 * AutoGen 
 * 
*/

using UnityEngine;
using System.Collections;
using RakNet;

public static class UpPlayer
{
    public static void SendPlayerGetUserid(int channelUserid,byte channelID)
    {
        //RakNet.BitStream bs = new RakNet.BitStream();

        //bs.WriteCompressed(channelUserid);
        //bs.WriteCompressed(channelID);
        //Dispatcher.Instance.Send(MessageID.ID_PLAYER_GET_USERID, bs);
    }


}